# lusterpass + direnv integration
# Copy to .envrc in your project root.
# Requires: lusterpass installed, lusterpass login done, lusterpass pull run at least once.
eval $(lusterpass env --profile dev)
